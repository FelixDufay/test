#include <stdlib.h>

#define ADDITION(a,b) (a+b)
#define SOUSTRACTION(a,b) (a-b)
#define MULTIPLICATION(a,b) ((a)*(b)
#define DIVISION(a,b) ((a)/(b)

int comparaison(int a, int b);
int simplification(int *a, int *b);
int pgcd (int a, int b);
